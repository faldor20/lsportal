(library
 (name jsonrpc_runner)
 


 (preprocess(pps ppx_jane ))
  (libraries base eio ppx_jane stdio jsonrpc yojson ppx_yojson_conv_lib lsp dyn )
  )
(env (dev (flags (:standard -warn-error -A))))
(include_subdirs unqualified)
